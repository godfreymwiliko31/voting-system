node_modules/
.env
data/
*.log
.DS_Store
Thumbs.db

# Local deploy bundles (build from repo on Render instead)
UCHAGUZI-deploy.zip
UCHAGUZI-deploy.tar.gz

# SQLite (uncomment if you never want the live DB in git)
# voting.db
